# Compatibility stub: calls the main lobby function.  This function exists to
# avoid "Unknown function lobby:teleport_lobby" errors if something else on the
# server is still referencing the old function name.
function lobby:lobby